{
  "name": "entity-review-deploy",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply REVIEW_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply REVIEW_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.41.0"
  }
}

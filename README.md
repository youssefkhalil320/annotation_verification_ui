# Entity Review

Static deployment of the physician annotation interface with all 240 ACI Bench documents. `index.html` is the entry point.

Review state is stored locally in each annotator's browser. Use the Export menu to download reviewed JSON files.

# Entity Review

Static deployment of the physician annotation interface with 240 ACI Bench documents and 200 representative CADEC forum documents. `index.html` is the entry point.

Review state is stored locally in each annotator's browser. Use the Export menu to download reviewed JSON files.
